<%= form_with(model: purchase, local: true) do |form| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :purchase_user_id %>
  </div>

  <div class="field">
    <%= form.label :provider_id %>
    <%= form.text_field :provider_id, id: :purchase_provider_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :purchase_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
