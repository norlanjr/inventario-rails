<%= form_for(@entry) do |form| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this @entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :entry_user_id %>
  </div>

  <div class="field">
    <%= form.label :provider_id %>
    <%= form.text_field :provider_id, id: :entry_provider_id %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :entry_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
