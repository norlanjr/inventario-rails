<p id="notice"><%= notice %></p>

<h1>Compras</h1>

<table class="table table-striped">
  <thead class="thead-dark">
    <tr>
      <th>N°Compra</th>
      <th>User</th>
      <th>Proveedor</th>
      <th>Total</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= entry.id %></td>
        <td><%= entry.user.email %></td>
        <td><%= entry.provider.nombre%></td>
        <td><%= entry.total %></td>
        <td>
          <%= link_to 'Detalle', entry, class: "btn btn-info"%>
          <%= link_to 'Edit', edit_entry_path(entry), class: "btn btn-secondary" %>
          <%= link_to 'Destroy', entry, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>

        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<button  type="submit" class="btn btn-lg btn-primary"><%= link_to 'Nueva', new_entry_path %></button>
